"""Capture pipeline.

Deliberately empty: `session` needs Qt and `decoder` needs ffmpeg, while
`h264` and `engines` are useful (and testable) without either. Import the
submodule you actually want.
"""
